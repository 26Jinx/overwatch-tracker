{
  "name": "overwatch-tracker",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "migrate": "python3 scripts/migrate.py",
    "ports": "lsof -nP -iTCP:3001 -iTCP:5173 -sTCP:LISTEN",
    "stop": "lsof -ti tcp:3001 -ti tcp:5173 | xargs kill 2>/dev/null; echo 'overwatch dev servers stopped'"
  },
  "workspaces": ["client", "server"],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
